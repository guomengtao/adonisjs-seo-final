name: Keep HF Space Alive

on:
  schedule:
    # 每天 UTC 时间 0:00 运行（北京时间早上 8:00）
    - cron: '0 0 * * *'
  workflow_dispatch: # 允许手动触发测试

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping HF Space
        run: |
          curl -I https://mengtaoguo-missing-persons-space.hf.space